server {
    listen 80;
    server_name _;
    server_tokens off;

    root /usr/share/nginx/html;
    index index.html;

    location ^~ /_api/ {
        return 404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
