/**
 * Stable re-export facade for shared VFS file UI. First-party apps import this
 * as `@daopk/files`, and the host import map points that bare specifier at this
 * entry's hashed runtime chunk.
 */
export * from "~/components/files";
