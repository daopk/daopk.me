allowBuilds:
  "@parcel/watcher": false
  esbuild: true
  sharp: true
  vue-demi: false
  # workerd ships wrangler's local dev runtime; deploys don't need its binary,
  # so skip the heavy postinstall download.
  workerd: false
packages:
  - .
  - apps/*
